-- Views: Shorthands for SELECT queries
-- Views can be used to define shorthands:
-- Views act like tables, but are defined by a SELECT statement:

CREATE VIEW viewname
AS SELECT query;

-- Simple views are typically updatable